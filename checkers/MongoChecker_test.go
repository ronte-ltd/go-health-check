package checkers

import (
	"testing"
)

func TestMongoCheckerUpStatus(t *testing.T) {
	//sess, err := mgo.Dial("127.0.0.1:27017/local")
	//if err != nil {
	//	t.Fatalf("Cannot connect to DB: %s", err.Error())
	//}
	//defer sess.Close()
	//mongoChecker := NewMongoChecker("Mongo", sess)
	//health, err := mongoChecker.Check()
	//if err != nil {
	//	t.Fatalf("Unhealthy: %s", err.Error())
	//}
	//t.Logf("Health: %+v", health)
}
